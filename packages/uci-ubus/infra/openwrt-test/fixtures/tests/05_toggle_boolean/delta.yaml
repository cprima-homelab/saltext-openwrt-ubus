description: "Enable recording on encoder (boolean-like 0 to 1)"
function: uci_ubus.managed
kwargs:
  config: testcorpus
  sections:
    obs_box:
      _type: encoder
      recording: '1'
expect: changed
