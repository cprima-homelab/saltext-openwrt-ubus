description: "Change timezone on anonymous studio singleton via _type resolution"
function: uci_ubus.managed
kwargs:
  config: testcorpus
  sections:
    _studio:
      _type: studio
      timezone: 'America/New_York'
expect: changed
