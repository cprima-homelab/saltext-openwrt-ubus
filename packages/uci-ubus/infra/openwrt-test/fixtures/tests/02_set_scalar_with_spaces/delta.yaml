description: "Change resolution (space-embedded scalar) on named camera"
function: uci_ubus.managed
kwargs:
  config: testcorpus
  sections:
    overhead_cam:
      _type: camera
      resolution: '1920 1080'
expect: changed
